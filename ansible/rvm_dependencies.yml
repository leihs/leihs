---
- hosts: vagrant
  sudo: True
  tasks:
  - name: Install Ruby compilation dependencies
    apt: pkg={{ item }} state=present
    with_items:
      - libssl-dev
      - libreadline6-dev
      - build-essential
      - curl
      - bash
    tags:
      - packages
