---
- hosts: vagrant
  user: vagrant
  roles:
    - ci_servers
    - mariadb_servers
    - rails_servers
  tasks:
    - include: tasks/leihs_packages.yml
    - include: tasks/leihs_virtual_host.yml
  handlers:
    - include: handlers/reload_apache.yml
