class RemoveComments < ActiveRecord::Migration
  def up

    # TODO run in future
    # currently the same database serves both leihs2 and leihs3, 
    # so we will run this migration just after we are switching off leihs2
    # and the database is not used in parallel  

    drop_table :comments
  end

  def down
  end
end
