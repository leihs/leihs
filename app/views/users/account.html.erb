Ext.onReady(function(){

	<%= render :partial => '/users/events' %>
	<%= render :partial => '/users/orders' %>
	<%= render :partial => '/users/contracts' %>

	center_tabs = Ext.getCmp('center_tabs');

	account_panel = center_tabs.getComponent('account_panel');
	if(!account_panel){
    account_panel = new Ext.TabPanel({
		id: 'account_panel',
        title: '<%= _("My Orders") %>',
		closable: true,
	    autoScroll: true,
		activeTab: 0,
		items: [events_grid, orders_grid, contracts_grid
/*		,
		{ xtype: 'panel',
	        title:'Doc 2',
			closable: true,
		    autoScroll:true,
			listeners: {
				activate: function(){
					this.load({
					    url: '/frontend/index2',
					    text: '<%= _("Loading") %>...',
					    timeout: 30,
						scripts: true
					});
				}
			}
		  }
*/
		  ] 
    });
	center_tabs.add(account_panel);
}
center_tabs.activate(account_panel);

});