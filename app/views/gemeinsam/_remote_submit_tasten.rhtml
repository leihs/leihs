<%
submit_text = 'Ändern' if submit_text.blank?
div_id = 'div_remote_form' if div_id.blank?
-%>
<div class="text_right">
	<div id="remote_submit_tasten">
	<%= link_to_function( 'Abbrechen',
				"Element.toggle('#{div_id}')" ) %>
	|
	<%= submit_tag submit_text %>
	</div>
	<div id="remote_submit_waiting" style="display:none;">
		...moment bitte
	</div>
</div>