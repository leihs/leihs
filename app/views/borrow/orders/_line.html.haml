.row.line{:"data-id" => line.id}

  - unless line.available?
    .line-info.red

  .line-col.col1of9.padding-left-l

    %img.max-height-xxs-alt.max-width-xs{src: model_image_thumb_path(line.model.id)}

  .line-col.col5of9.text-align-left

    .col1of10.text-align-right.padding-right-s
      = line.quantity
      x

    %strong.col6of10.text-ellipsis{:title => "#{line.model.name}"}= line.model.name

    %strong.col2of10.text-ellipsis.padding-left-m{:title => "#{line.model.manufacturer}"}= line.model.manufacturer

  .line-col.col2of9.text-align-left
    = interval(line.start_date, line.end_date)
    = _("until")
    %span{class: line.inventory_pool.is_open_on?(line.end_date) ? nil : "emboss red"}
      = short_date(line.end_date)

  .line-col.line-actions.col1of9
    = link_to borrow_order_remove_lines_path(line_ids: [line.id]), method: :delete, data: { confirm: _("Are you sure?") }, class: "button red margin-right-m" do
      .icon.delete
      = _("Delete")