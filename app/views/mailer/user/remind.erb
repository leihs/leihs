<%= _("Dear leihs user,") %>

<%= _("This is an automatic e-mail sent to you by the leihs system.") %> 

<%= _("The following items are overdue. Please return them as soon as possible:") %>

<% @visits.each do | v | %>

  <%= v.quantity %> <%= _(" item(s) due on ") %><%= v.date %><%= _(" at the inventory pool ") %><%= v.inventory_pool.name %>
	<% v.contract_lines.each do |l| %>
	* <%= l.quantity %> <%= l.model.name %> (<%= l.item.inventory_code %>), <%= l.start_date %>-<%= l.end_date %>
	
	<% end %>
<% end %>

<%= _("By not returning these items, you might be blocking other people's reservations. This is very unfair to the others and you are causing a significant amount of work for the inventory managers.") %>

<%= _("Even if you return the items now, the inventory manager may fine or otherwise penalize you.") %>

<%= _("Please coordinate with the inventory managers in charge to find a date and time for the return.") %>

<%= _("Kind regards,") %>

<%= _("The leihs system") %>
