<%= _("Dear leihs user,") %>

<%= _("This is an automatic e-mail sent to you by the leihs system.") %> 

<%= _("The following items are due to be returned tomorrow:") %>

<% @visits.each do | v | %>
  <%= v.quantity %> <%= _(" item(s)") %><%= _(" at the inventory pool ") %><%= v.inventory_pool.name %>
	<% v.contract_lines.each do |l| %>
	  * <%= l.quantity %> <%= l.model.name %> (<%= l.item.inventory_code %>), <%= l.start_date %>-<%= l.end_date %>
	  
	<% end %>
<% end %>

<%= _("Someone else might already be waiting for some of these items, so we are just sending you this little reminder to make sure you don't forget.") %>

<%= _("Kind regards,") %>

<%= _("The leihs system") %>
