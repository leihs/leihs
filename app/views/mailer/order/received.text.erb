<%= _("Dear leihs manager,") %>

<%= _("** This is an automatically generated response **") %>

<%= _("An order for the following items listed below was received in an inventory pool you are responsible for.") %> 

<%= _("Inventory pool") + ": " + @order.inventory_pool.name unless @order.inventory_pool.nil? %>
<%= _("User") + ": " + @order.user.to_s unless @order.user.nil? %>

<% @order.lines.each do | line | %>
	<%= line.quantity%> <%= line.model.name %>, <%= line.start_date %> - <%= line.end_date %>
<% end %>

<%= _("This order is still pending. Please log in to your leihs system and either approve or reject it.") %> 

<%= _("The user who placed this order has received a similar e-mail message informing them that the order first needs to be approved before it is valid.") %> 

<%= _("Kind regards,") %>

<%= Setting::EMAIL_SIGNATURE %>
--
<%= @order.inventory_pool.description unless @order.inventory_pool.nil? %>

