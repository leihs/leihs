<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01//EN"
   "http://www.w3.org/TR/html4/strict.dtd">
<html>
   <head>
      <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
      <title>leihs - Order received</title>
   </head>
   <body>
      <p><%= _("Dear leihs manager,") %></p>
      <p><%= _("** This is an automatically generated response **") %></p>
      <p><%= _("An order for the following items listed below was received in an inventory pool you are responsible for.") %></p>

      <h3><%= _("Inventory pool") + ": " + @order.inventory_pool.name unless @order.inventory_pool.nil? %></h3>
      <p><%= _("User") + ": " + @order.user.to_s unless @order.user.nil? %></p>

      <ul>
        <% @order.lines.each do | line | %>
          <li><%= line.quantity%> <%= line.model.name %>,  <%= line.start_date %> - <%= line.end_date %></li>
        <% end %>
      </ul>

      <p><%= _("This order is still pending. Please log in to your leihs system and either approve or reject it.") %></p>

      <p><%= _("The user who placed this order has received a similar e-mail message informing them that the order first needs to be approved before it is valid.") %> </p>

      <p><%= _("Kind regards,") %></p>
      <p><%= Setting::EMAIL_SIGNATURE %></p>
      <hr />
      <p><%= @order.inventory_pool.description.gsub(/\n/, '<br>') unless (@order.inventory_pool.nil? or @order.inventory_pool.description.blank?) %></p>
   </body>
</html>
