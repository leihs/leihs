<%= _("Dear leihs client,") %>

<%= _("** This is an automatically generated response **") %>

<%= _("Your order for the following items listed below was successfully submitted.") %> 

<%= _("Inventory pool") + ": " + @order.inventory_pool.name unless @order.inventory_pool.nil? %>

<% @order.order_lines.each do | line | %>
	<%= line.quantity%> <%= line.model.name %>, <%= line.start_date %> - <%= line.end_date %>
<% end %>

<%= _("Unfortunately your order is still pending, but you will soon receive a confirmation of order by separate e-mail. You can view the status of your order through your leihs account.") %> 

<%= _("Kind regards,") %>

<%= Setting::EMAIL_SIGNATURE %>
--
<%= @order.inventory_pool.description unless @order.inventory_pool.nil? %>

