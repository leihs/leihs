<% content_tag :li do %>
	<%= link_to icon_tag("package") + " " + _('Models in this pool') + " (#{current_inventory_pool.models_active.count})", backend_inventory_pool_models_path(current_inventory_pool) %>
	<%# OPTIMIZE 0501 search_count %>
<% end %>
