#page.insert_html :top, 'changes', "<li>#{@change}</li>" 
#page.visual_effect :highlight, 'changes'

if @contract_line
  page['line_item_inventory_code_' + params[:contract_line_id]].value = @contract_line.item.inventory_code if @contract_line.item

  css_class = "line_valid_#{@contract_line.complete?}"
  page << "$('inventory_code_#{params[:contract_line_id]}').className='#{css_class}'"
  
  page.replace_html 'errors', :partial => 'errors'
end

page << "$('inventory_code').focus()"
