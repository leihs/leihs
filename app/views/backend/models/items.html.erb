
<% tabnav :model_edit do %>

	<ul>
	<% for item in (@model.items & @current_inventory_pool.items) %>
		<li><b><%= item.inventory_code %></b> <%= item.serial_number %></li>
	<% end %>
	</ul>

	<%= render :active_scaffold => 'backend/items', :constraints => { :model_id => @model.id } %>

<% end %>