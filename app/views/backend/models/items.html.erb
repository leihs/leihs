
<% tabnav :model_edit do %>

	<ul>
	<% for item in (@model.items & @current_inventory_pool.items) %>
		<li><b><%= link_to item.inventory_code, :controller => 'items', :action => 'show', :id => item.id %></b> <%= item.serial_number %>, <%= item.location %></li>
	<% end %>
	</ul>

<% end %>