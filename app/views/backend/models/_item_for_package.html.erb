<%= item_for_package.model.name %> <%= item_for_package.model.manufacturer %>, <%= item_for_package.inventory_code %>

<%= link_to _('Add to package'),
			:action => 'add_package_item',
			:id => @model,
			:item_id => item_for_package %>

<br />
