<% table_with_search_and_pagination :columns => [[_("Model"), 'models.name'], [_("Inventory Code"), 'items.inventory_code'], [_("Serial Number"), 'items.serial_number'], _("Location"), [_("Borrowable"), 'items.is_borrowable'], _("In stock?"), _("Action")],
						   			:records => @items do |item| %>	

			<tr>
				<td><%= item.model.name %></td>
				<td><%= item.inventory_code %></td>
				<td><%= item.serial_number %></td>
				<td><%= item.location.to_s %></td>
				<td><%= image_tag($layout_public_path + "/images/icons/" + (item.is_borrowable ? "accept": "delete") + ".png") %></td>
				<td><%= image_tag($layout_public_path + "/images/icons/" + (item.in_stock? ? "accept": "delete") + ".png") %></td>
				<td class="buttons" style="white-space: nowrap">
					<%= link_to _("Show"), [:backend, @current_inventory_pool, item.model, item] %>
				</td>
			</tr>

<% end %>
