
<!-- start iterator -->
<%
last_date = Date.new
for @line in @contract_lines
  if @line.end_date > last_date
    last_date = @line.end_date
	content_tag :tr, :class => "line_header" do
		style = (@line.end_date < Date.today ? "color: red;" : nil)
		content_tag :td, :colspan => 5, :class => "title", :style => style do %>
			<%= short_date(@line.end_date) %>
		<% end
	end 
  end %>
	<%= render :partial => @line.type.underscore %>
<% end %>
<!-- end iterator -->
						