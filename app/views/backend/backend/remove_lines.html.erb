	<h2><%= _("Do you really want to delete the object?") %></h2>
	<br />
	
	<% @lines.each do	|l| %>
		<%= l.model.name %>
		<br />
	<% end %>

	<br />		
	<div class="buttons">


		<%=	link_to(image_tag($layout_public_path + "/images/icons/cancel.png") + " " + _("Delete"),
			 { :action => 'remove_lines', :id => params[:id], :lines => @lines },
			 :class => 'negative', :method => 'post', :target=> '_top') %>
			
					 
		<%=	cancel_popup_button(_("Cancel") ) %>
	</div>	
	