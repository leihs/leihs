= link_to _("All"), admin_users_path, class: "inline-tab-item #{"active" unless @role == "admins"}"
= link_to _("Administrator"), admin_users_path(params: {role: "admins"}), class: "inline-tab-item #{"active" if @role == "admins"}"
