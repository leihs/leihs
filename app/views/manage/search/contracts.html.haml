= render partial: 'content_for', locals: { container_title: _("Contracts") }

#contracts-search-results.wrapper.margin-top-m

  = render :partial => 'manage/tabs/search', :locals => {:active_tab => :contract_search}

  = render 'search_results_title'

:coffeescript
  
  jQuery ->
    new App.SearchResultsContractsController
      el: $("#contracts-search-results")
      searchTerm: #{@search_term.to_json}
