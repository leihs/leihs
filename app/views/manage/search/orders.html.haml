= render partial: 'content_for', locals: { container_title: _("Orders") }

#orders-search-results.wrapper.margin-top-m

  = render :partial => 'manage/tabs/search', :locals => {:active_tab => :order_search}

  = render 'search_results_title'

:coffeescript
  
  jQuery ->
    new App.SearchResultsOrdersController
      el: $("#orders-search-results")
      searchTerm: #{@search_term.to_json}
