= render partial: 'content_for', locals: { container_title: _("Software") }

#software-search-results.wrapper.margin-top-m

  = render :partial => 'manage/tabs/search', :locals => {:active_tab => "software_search".to_sym}

  = render 'search_results_title'

:coffeescript
  
  jQuery ->
    new App.SearchResultsSoftwareController
      el: $("#software-search-results")
      searchTerm: #{@search_term.to_json}
