- if current_inventory_pool
  = render :partial => 'manage/tabs/admin_inside_ip', :locals => {:active_tab => active_tab}
- else
  = render :partial => 'manage/tabs/admin_outside_ip', :locals => {:active_tab => active_tab}
