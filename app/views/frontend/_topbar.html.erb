		var user_resume = new Ext.Panel({
									title: '<%= current_user.login %>',
									closable: true,
		                            autoScroll: true,
									frame: true,
									listeners: {
										activate: function(cmp) {
											cmp.load({ url: '/user' });
										}				
									}			
								});

	    var topbar = new Ext.Toolbar({
			height: 40,
			style: 'color: navy;',
	        items: [{
					xtype: 'tbtext',
					text:'<a href="/" id="logo">leihs 2.1</a>'
				}, '->',
						{text: '<%= _("Help") %>', 
						  iconCls:'bt',
				          icon:'<%= icon_path('help') %>',
						  handler: function() {
								  	center_tabs.add(help_panel);
									center_tabs.activate(help_panel);
								}
						},
					'-',
						<% if current_user.has_role?('manager', nil, false) %>
						{text: 'Backend', 
						  iconCls:'bt',
				          icon:'<%= icon_path('application_edit') %>',
						  handler: function() {
									window.location.href = '/backend';
								}
						},
						<% end %>
					'-',
						<% Language.active_languages.each do |l| %>
	                        {text: '<%= l.name %>',
							 iconCls:'bt',
					         icon:'<%= icon_path('comment') %>',
							 handler: function() {
										window.location.href = '<%= url_for({:locale => l.locale_name}) %>';
									}
							},
						<% end %>
					'-',
						{text: '<%= current_user.login %>', 
						  iconCls:'bt',
				          icon:'<%= icon_path('user') %>',
						  handler: function() {
									new Ext.data.ScriptTagProxy({
										    url: '/user/account'
										}).load();
								}
						},
                        {text: '<%= _("Logout") %>',
						 iconCls:'bt',
				         icon:'<%= icon_path('application_delete') %>',
						 handler: function() {
									window.location.href = '/logout';
								}
						}
				]
	    });
