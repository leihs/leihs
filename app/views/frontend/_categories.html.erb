
	  // create initial root node
      var categories_root = new Ext.tree.AsyncTreeNode({
        text: 'Invisible Root',
        id:'0'
      });
      
	  // create the tree
      var categories_panel = new Ext.tree.TreePanel({
        loader: new Ext.tree.TreeLoader({
          url:'/models/categories.ext_json',
          requestMethod:'GET'
        }),
		title: 'Categories',
//        useArrows:true,
		collapsible: true,
		border: false,
		animate:true,
		autoScroll:true,
//		renderTo:'categories',
        root: categories_root,
        rootVisible:false,
		listeners: {
			click: function( node, e ){
				node.toggle();
				models_grid.show();
				models_store.load({params:{start:0, limit:25, category_id: node.id}}); // TODO 18** start and limit as default, then:  ({params:{category_id: node.id}});
			}
		}
      });
	  
	  // expand invisible root node to trigger load
      // of the first level of actual data
      //categories_root.expand();
