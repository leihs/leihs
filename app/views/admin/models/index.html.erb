<% if @show_categories_tree %>  

	<%= extjs_model_tree %>
	  
<table><tr>
<td id="categories" style="vertical-align:top;" width="20%">

</td>
<td style="vertical-align:top;" width="80%">
<% end %>  

<% unless request.xml_http_request? or @model %>

<div class="toolbar-second buttons">
	<%= link_to _("Create New"), :action => 'new' %>
</div>

<% end %>


<% table_with_search_and_pagination :columns => ["", _("Model"), _("Manufacturer"), _("Items"), _("Compatibles"), _("Action")],
						   			:records => @models do |model| %>	

			<tr>
				<td><%= image_tag(model.images.first.public_filename(:thumb)) unless model.images.empty? %></td>
				<td><%= model.name %></td>
				<td><%= model.manufacturer %></td>
				<td><%= model.items.size %></td>
				<td><%= model.compatibles.size %></td>
				<td class="buttons" style="white-space: nowrap">
					<ul>
						<li><%= link_to _("Show"), admin_model_path(model) %></li>
						<li><%= link_to _("Delete"), admin_model_path(model),
										:confirm => _("Are you sure?"),
										:method => :delete if model.items.empty? %></li>
					</ul>
				</td>
			</tr>


<% end %>

<% if @show_categories_tree %>  
</td>
</tr></table>
<% end %>  
