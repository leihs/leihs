.visit
  .date
    {{if moment().sod().diff(moment(date).sod(),"days") == 0}}
    %strong= _("Today")
    {{/if}} 
    ${moment(date).format(i18n.date.XL)}
  {{if lines.length>0}}
  ${$grouped_lines = new GroupedLines(lines), ""}
  {{tmpl($grouped_lines) "tmpl/linegroup"}}
  {{/if}}
  %hr