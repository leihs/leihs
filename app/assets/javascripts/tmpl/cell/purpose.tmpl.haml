{{if description}}    
{{if String(description).length > 52}}
%li.purpose.hastip
  %span.text
    {{html String(description).substring(0, 52) }}
    {{if String(description).length > 52}}
    {{html "..."}}
    {{/if}}
  .tip
    %p
      ${description}
{{else}}
%li.purpose
  %span.text
    ${description}
{{/if}}
{{else}}
%li.purpose
{{/if}}