%ul{:class => "line ${type} take_back"}
  
  .main  
  
    %li.select
      %label
        %input{:type => "checkbox"}
        
    %li.inventory_code
      ${item.inventory_code}
            
    %li.name
      %strong ${model.name}