%ul.line{:class => "${type}"}
  .container  
    %li.select
      %label
        = check_box_tag "selected"
        
    %li.inventory_code
      ${model.inventory_code}
      
    %li.quantity
      %input{:type => "text", :value => "${quantity}"}
        
    %li.name
      ${model.name}
