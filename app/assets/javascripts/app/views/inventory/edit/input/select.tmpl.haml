%select{:name => "${formName}"}
  {{each(i,option) values}}
  {{if String($item.data.value) == String(option.value)}}
  %option{:value => "${option.value}", :selected => true}
    ${_jed(option.label)}
  {{else}}
  %option{:value => "${option.value}"}
    ${_jed(option.label)}
  {{/if}}
  {{/each}}