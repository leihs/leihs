%div.field-inline-entry
  %input{:type => :hidden, :name => "user[groups][][id]", :value => "${id}"}
  %span.text-ellipsis{:width => "100"} ${name}
  .floatright
    %div{:style => "display: inline-block;"}
      %a.clickable.remove ${_jed("Remove")}
