jobs:

  tests-leihs:

    name: Leihs Tests

    description: |
      Run leihs features (except engines) under MRI

    context:
      task-defaults:
        max-auto-trials: 3

      _cider-ci_include:
        - .cider-ci/shared/tests-context.yml
        - .cider-ci/tasks/cucumber_leihs_scenarios.yml
